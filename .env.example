# Copy to .env and fill in. .env is gitignored — never commit real credentials.

BASE_URL=https://cosmoscitizenapp-b3h4dqh7bte2ezdw.southindia-01.azurewebsites.net

# No account is needed. Every test in this suite runs signed out, because the
# portal's only way in is a 6-digit code sent to a real inbox or phone — see the
# note on the `signed-out` project in playwright.config.ts.
